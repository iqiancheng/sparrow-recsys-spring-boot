version: '2'
services:
  sparrow:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: recsys
    volumes:
      - /etc/localtime:/etc/localtime
    ports:
      - "6010:8080"
    tty: true
    environment:
      - TZ=Asia/Shanghai
